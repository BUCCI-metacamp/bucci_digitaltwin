using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;
using Factory;

public class CountText : MonoBehaviour
{
    public TextMeshProUGUI textOut;
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //string str = string.Format("{0},::{1}", GameObject.Find("CASE").GetComponent<ManageCase2>().GetIndex(), GameObject.Find("CASE").GetComponent<ManageCase2>().GetBool());
        //textOut.text = str;
    }
}
